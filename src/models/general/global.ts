export interface serviceModel {
  title: string;
  subTitle: string;
  icon: string;
  alt: string;
}
export interface footerLink {
  href: string;
  linkTitle: string;
}

export interface drawerMenu {
  name?: string;
  url?: string;
  id?: any;
}
